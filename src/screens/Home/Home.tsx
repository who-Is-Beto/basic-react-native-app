import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Home: React.ReactNode = (): JSX.Element => {
  return (
    <View>
      <Text>Home</Text>
    </View>
  );
};

export default Home;

const styles = StyleSheet.create({});
